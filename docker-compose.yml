services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: dracma
      POSTGRES_PASSWORD: dracma
      POSTGRES_DB: dracma
    ports:
      - "5432:5432"
    volumes:
      - dracma-pgdata:/var/lib/postgresql/data

volumes:
  dracma-pgdata:
