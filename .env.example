# PostgreSQL connection string.
# Local dev: matches docker-compose. On Railway: reference the Postgres
# service variable instead -> ${{Postgres.DATABASE_URL}}
DATABASE_URL="postgresql://dracma:dracma@localhost:5432/dracma"

# Session signing secret — REQUIRED in production.
# Generate with: openssl rand -base64 32
AUTH_SECRET="change-me"
